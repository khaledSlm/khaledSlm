# تعليمات تحميل وتثبيت تطبيق قاعدة بيانات القرآن الكريم

## 📦 الملف المضغوط

تم إنشاء ملف مضغوط يحتوي على جميع ملفات المشروع:
**اسم الملف:** `quran_database_app.tar.gz`
**الحجم:** 18 كيلوبايت

## 📋 محتويات الملف المضغوط:

### ملفات Python الأساسية:
- `database_schema.sql` - مخطط قاعدة البيانات
- `quran_database_builder.py` - برنامج بناء قاعدة البيانات
- `run_python_app.py` - التطبيق الرئيسي بواجهة رسومية
- `start_app.bat` - ملف التشغيل السريع

### ملفات Flutter (اختيارية):
- `pubspec.yaml` - إعدادات مشروع Flutter
- `lib/` - مجلد كود Flutter
  - `main.dart`
  - `models/quran_word.dart`
  - `database/database_helper.dart`
  - `screens/home_screen.dart`
  - `widgets/search_dialog.dart`
- `android/` - إعدادات Android
- `assets/` - الموارد والبيانات

### ملفات التوثيق:
- `README.md` - شرح المشروع
- `تعليمات_التشغيل_Python.md` - تعليمات Python
- `تعليمات_التشغيل_المبسطة.md` - تعليمات Flutter
- `SETUP_INSTRUCTIONS.md` - تعليمات الإعداد التفصيلية

## 🔧 خطوات التثبيت والتشغيل:

### الخطوة 1: تحميل وفك الضغط

#### على Windows:
1. حمّل الملف `quran_database_app.tar.gz`
2. استخدم برنامج مثل WinRAR أو 7-Zip لفك الضغط
3. أو استخدم Windows PowerShell:
   ```powershell
   tar -xzf quran_database_app.tar.gz
   ```

#### على Linux/Mac:
```bash
tar -xzf quran_database_app.tar.gz
```

### الخطوة 2: تثبيت Python (إذا لم يكن مثبت)
1. اذهب إلى: https://python.org
2. حمّل أحدث إصدار
3. ثبّته مع تفعيل "Add to PATH"

### الخطوة 3: تشغيل التطبيق

#### الطريقة السهلة:
- انقر نقراً مزدوجاً على `start_app.bat`

#### الطريقة اليدوية:
```bash
# افتح Command Prompt في مجلد المشروع
cd path/to/extracted/folder

# بناء قاعدة البيانات
python quran_database_builder.py

# تشغيل التطبيق
python run_python_app.py
```

## 🎯 خيارات التشغيل:

### الخيار 1: تطبيق Python (مُوصى به للبداية)
- **المتطلبات:** Python فقط
- **المميزات:** سهل التثبيت، يعمل فوراً
- **العيوب:** يعمل على الكمبيوتر فقط

### الخيار 2: تطبيق Flutter (متقدم)
- **المتطلبات:** Flutter SDK + Android Studio
- **المميزات:** يعمل على الكمبيوتر والهاتف
- **العيوب:** يحتاج إعداد أكثر

## 🔍 اختبار التطبيق:

بعد التشغيل، جرب البحث عن:
- `الله` (2699 مرة)
- `الرحمن` (57 مرة)
- `الرحيم` (114 مرة)
- `الحمد` (38 مرة)

## 📁 هيكل المجلد بعد فك الضغط:

```
quran_database_app/
├── database_schema.sql
├── quran_database_builder.py
├── run_python_app.py
├── start_app.bat
├── تعليمات_التشغيل_Python.md
├── lib/
│   ├── main.dart
│   ├── models/
│   ├── database/
│   ├── screens/
│   └── widgets/
├── android/
├── assets/
└── README.md
```

## ⚠️ ملاحظات مهمة:

1. **تأكد من تثبيت Python** قبل التشغيل
2. **الملفات العربية** قد تحتاج ترميز UTF-8
3. **قاعدة البيانات** ستُنشأ تلقائياً عند أول تشغيل
4. **البيانات الحالية** تجريبية ومحدودة

## 🆘 استكشاف الأخطاء:

### مشكلة: "Python is not recognized"
**الحل:** ثبّت Python وأضفه للمسار

### مشكلة: "No module named 'tkinter'"
**الحل:** 
- Windows: tkinter مثبت افتراضياً
- Linux: `sudo apt-get install python3-tk`

### مشكلة: النص العربي لا يظهر
**الحل:** تأكد من دعم النظام للخطوط العربية

## 📞 للدعم:

إذا واجهت مشاكل:
1. اقرأ ملف `تعليمات_التشغيل_Python.md`
2. تحقق من رسائل الخطأ
3. تأكد من تثبيت Python بشكل صحيح

## 🚀 التطوير المستقبلي:

يمكن تطوير التطبيق بإضافة:
- المزيد من آيات القرآن الكريم
- ميزات بحث متقدمة
- واجهة أجمل
- دعم أفضل للغة العربية

---

**ملاحظة:** هذا مشروع تعليمي ومفتوح المصدر. يمكن تطويره وتحسينه حسب الحاجة.
