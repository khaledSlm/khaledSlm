# تعليمات تشغيل تطبيق قاعدة بيانات القرآن الكريم - Python

## الملفات الموجودة:

### 1. ملفات قاعدة البيانات:
- `database_schema.sql` - مخطط قاعدة البيانات
- `quran_database_builder.py` - برنامج بناء قاعدة البيانات

### 2. ملفات التطبيق:
- `run_python_app.py` - التطبيق الرئيسي بواجهة رسومية
- `start_app.bat` - ملف تشغيل مبسط

## خطوات التشغيل السريعة:

### الطريقة الأسهل:
1. **انقر نقراً مزدوجاً** على ملف `start_app.bat`
2. انتظر حتى يتم بناء قاعدة البيانات
3. ستفتح نافذة التطبيق تلقائياً

### إذا لم تعمل الطريقة الأولى:

#### 1. تثبيت Python (إذا لم يكن مثبت):
- اذهب إلى: https://python.org
- حمّل أحدث إصدار من Python
- ثبّته مع تفعيل خيار "Add to PATH"

#### 2. تشغيل التطبيق يدوياً:
```bash
# افتح Command Prompt في مجلد المشروع
# اكتب هذه الأوامر:

# بناء قاعدة البيانات
python quran_database_builder.py

# تشغيل التطبيق
python run_python_app.py
```

## كيفية استخدام التطبيق:

### 1. الواجهة الرئيسية:
- نافذة بسيطة مع حقل البحث
- زر البحث
- منطقة عرض النتائج

### 2. البحث:
- اكتب الكلمة في حقل البحث
- اضغط زر "بحث" أو اضغط Enter
- ستظهر النتائج في المنطقة السفلية

### 3. النتائج تشمل:
- الكلمة المطابقة
- عدد مرات ذكرها في القرآن
- السور والآيات التي تحتوي عليها
- جزء من نص الآية

## أمثلة للبحث:

جرب البحث عن هذه الكلمات:
- `الله`
- `الرحمن`
- `الرحيم`
- `الحمد`
- `رب`

## ميزات التطبيق:

### ✅ ما يعمل:
- البحث في الكلمات
- عرض النتائج مع التفاصيل
- واجهة عربية بسيطة
- قاعدة بيانات محلية (SQLite)

### 🔄 يمكن تطويره:
- إضافة المزيد من آيات القرآن
- تحسين البحث (البحث الجزئي)
- إضافة ميزات متقدمة
- تحسين التصميم

## استكشاف الأخطاء:

### مشكلة: "Python is not recognized"
**الحل:** Python غير مثبت أو غير مضاف للمسار
- ثبّت Python من python.org
- تأكد من تفعيل "Add to PATH" أثناء التثبيت

### مشكلة: "No module named 'tkinter'"
**الحل:** tkinter غير مثبت
```bash
# على Ubuntu/Debian:
sudo apt-get install python3-tk

# على Windows: tkinter مثبت افتراضياً مع Python
```

### مشكلة: خطأ في قاعدة البيانات
**الحل:** احذف ملف `quran_database.db` وأعد تشغيل التطبيق

### مشكلة: النص العربي لا يظهر بشكل صحيح
**الحل:** تأكد من دعم النظام للخطوط العربية

## إضافة بيانات جديدة:

لإضافة المزيد من آيات القرآن:
1. افتح ملف `quran_database_builder.py`
2. عدّل قائمة `fatiha_ayahs` لتشمل المزيد من الآيات
3. أو أضف سور جديدة في `surahs_data`
4. احذف ملف `quran_database.db`
5. أعد تشغيل التطبيق

## الملفات المُنتجة:

بعد التشغيل ستجد:
- `quran_database.db` - ملف قاعدة البيانات
- ملفات مؤقتة أخرى

## للمطورين:

### هيكل قاعدة البيانات:
- `surahs` - جدول السور
- `ayahs` - جدول الآيات  
- `words` - جدول الكلمات
- `word_occurrences` - مواضع الكلمات
- `word_statistics` - إحصائيات الكلمات

### إضافة ميزات جديدة:
- عدّل ملف `run_python_app.py`
- أضف وظائف جديدة في `QuranDatabaseBuilder`

## الدعم:

إذا واجهت مشاكل:
1. تأكد من تثبيت Python بشكل صحيح
2. جرب حذف قاعدة البيانات وإعادة إنشائها
3. تحقق من رسائل الخطأ في Command Prompt
